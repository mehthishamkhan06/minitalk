int main(int argc, char *argv[])
{
    int pid;

    (void)argv;
    if (argc == 1)
        //print the pid with newline and wait for the messages from client
    else
        //print error too few commands.
}